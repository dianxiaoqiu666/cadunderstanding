# 当前网页进程与 GLM 接入效果核验

检查时间：2026-09-10 15:03（北京时间）。

结论：服务运行正常，GLM-4.6V 已产生真实模型返回，输出已进入当前网页与下载 JSON；当前结果只覆盖一个局部区域，未完成整店可用区域识别。

## 服务与实际调用

- 页面：http://127.0.0.1:8123/；服务版本 1.3.0。
- PID 3916，启动时间与 `runtime/server.json` 一致，工作目录为本项目，监听 8123，`/health` 返回 HTTP 200 / ok。
- 配置为 `openai_compatible`、`https://open.bigmodel.cn/api/paas/v4`、`glm-4.6v`，服务确认 Key 已配置；本次没有读取或输出明文 Key。
- 当前网页结果 ID：`9454e0f786ee30ab95f9c79e9897c028a74f07782eddb291307ede72b47162da`。
- 已保存结果标记 `origin=MODEL_API`、`analysis.status=COMPLETED`，请求与返回模型均为 `glm-4.6v`。
- 该次返回的 usage：输入 41,782、输出 3,673、总计 45,455 tokens。这是响应记录，不是平台账单核对。
- 输入为 `选定规划只留墙体.dxf`，SHA256 `0753b00fae27eaa1355b9a3274789079ad9d2b33b28ba343f9afe5516d91468d`。JSON、三张编号 PNG、提示词和 Schema 的保存哈希一致。

## 识别效果

| 项目 | 当前结果 | 判断 |
| --- | --- | --- |
| 构件 | 原始 76 WALL、18 ANNOTATION、10 UNKNOWN；整合后 68 WALL、8 COLUMN、18 ANNOTATION、10 UNKNOWN | 8 个填充实体被提出为柱候选，原始几何保持不变 |
| 柱的依据 | 8 条理由与输入中已有 COLUMN 候选理由逐字一致 | 没有提供新的判断依据，不能据此认定柱语义已确认 |
| 室内范围 | 1 个候选，7.410㎡ | 几何上与源拓扑 `region-7` 完全一致；网页中位于图纸右上方，只是局部闭合区域 |
| 障碍扣除 | 0 个、0㎡；8 个柱候选与该范围的占地交集面积均为 0 | 当前范围没有覆盖这些柱子所在的主要空间，不能把“扣除为零”解释为全店无障碍 |
| 构件关系 | 0 条 | 未增加门窗宿主等关系 |
| 可摆放状态 | `REVIEW_REQUIRED`、`MODEL_PROPOSAL`、`ready_for_placement=false` | 未批准用于自动摆放；整店室内语义和完整性仍未确认 |

因此，接口接通与 JSON 合法已得到证据支持；完整识别效果尚未达到整店空间布置的目标。不能把 7.410㎡作为整店面积，也不能用本次几何验证结果替代人工核实。

## 当前网页错误

网页保留一个 11 点室内圈定草稿，并显示“可用区域未通过室内包含与障碍相离校验”。服务日志有两次 `/v1/cad/analyze` HTTP 422，与界面错误一致。

该错误对应 `AREA_TOPOLOGY_INVALID`，发生在基础解析的区域求差/校验阶段；代码调用顺序是先完成基础解析，再调用模型。因此这两次失败不是 GLM 接口连接失败，也没有进入模型步骤。

页面上的 7.410㎡仍是之前成功请求的候选结果，不是这次 11 点圈定的新结果。本次没有刷新或修改用户页面，没有应用、取消或替换圈定。没有取得该草稿的精确坐标，尚未复现并定位几何失败根因；不能直接断定是圈定问题或浮点精度问题。

## 验证范围

本次 15 项检查全部通过：进程身份与端口、健康接口、配置、CAD 哈希、原始构件保留、证据来源与附件哈希、三图哈希、模型引用和几何校验重放、候选区域重算、三类 HTTP 下载一致性、候选不可直接授权摆放。浏览器读取的 warning/error 日志为空。

这是对已存在真实调用的离线重放与本机 HTTP 核验，本次新增付费模型调用为 0。没有修改生产代码、模型配置、源 CAD，也没有重启服务。本检查不是新的一次全量回归或 Human 验收。

证据：`outputs/glm-live-audit-2026-09-10/audit.json`；复核脚本：`.tmp/audit_current_ai.py`。

后续优先顺序：保留并取得失败圈定的精确输入，复现范围计算 422；随后改进模型对整图室内范围及缺口的判断与证据说明，避免仅复述已有几何候选；用同一实际 CAD 对照验证后再评价改进效果。
