# GLM-5.3-Flash 单次授权调用结果

2026-09-10。已按用户“授权调用”完成一次请求，取得 HTTP 200，118 秒后本机判定补全未完成。没有生成新的门店外围候选，没有自动重试。

## 本次实际证据

| 项目 | 记录 |
|---|---|
| 实验 | glm53flash-v2-01 |
| 请求时间 | 2026-09-10T08:33:31.324032Z，北京时间 16:33:31 |
| 请求地址 | https://open.bigmodel.cn/api/anthropic/v1/messages |
| 请求型号 | glm-5.3-flash |
| 实际发送材料 | 已冻结并校验哈希的 CAD JSON 和六张 PNG |
| 调用限制 | 1 次；300 秒等待；8192 最大输出 tokens；effort=max |
| 本机请求体发送完成 | 约 0.828 秒 |
| 收到 HTTP 200 响应头 | 约 117.984 秒 |
| 响应正文读取完成 | 约 118.000 秒 |
| 结果 | FAILED / LLM_OUTPUT_INCOMPLETE |
| 新外围/可用区域 | 无有效新提案 |
| 返回型号、服务端请求编号、usage、stop_reason | 本次未保存，无法从现有材料恢复 |

原始记录：`outputs/model-evaluations/glm53flash-v2-01/report.json`、`transport.json`。授权和输入核对记录：`outputs/model-diagnostics/glm53flash-v2-01-authorization.json`。综合核验：`outputs/model-diagnostics/glm53flash-v2-01-audit.json`。

本机网络日志证明收到这个请求的 HTTP 响应，不足以证明特定模型成功推理或具体计费。原程序先检查 `stop_reason == end_turn`，再保存回执；在缺少该字段或返回其他结束状态时，会提前抛出笼统错误。此次原响应没有落盘，进程已经结束，不能区分 max_tokens、拒绝、工具调用、协议不符或 HTTP 200 正文中的接口错误。不能直接认定 8192 输出上限是原因，不能把请求型号当成已验证的返回型号。

原实验的 17 个文件已核对哈希并保持原样；未用推测补写响应，也未修改原 CAD。网页原来的 7.410 m² 仍是早先局部结果，不属于本次试验。

## 已修复的诊断问题

1. 在检查结束状态和业务 JSON 之前保存脱敏回执，包括返回型号、用量、响应编号、结束状态和最终文本块。
2. 对输出截断、拒绝/工具调用、协议结构不符、HTTP 200 正文接口错误分别给出诊断；仍拒绝不完整结果，不生成假外围。
3. 失败回执同时进入评测文件、评测报告和 API 错误元数据；不保存密钥、原始错误消息或模型内部思考。

124 项离线回归通过：`outputs/global-vision-2026-09-10/tests-06.xml`。新增测试证明：即使截断响应含有看似完整的 JSON，也保持失败；模拟的结束原因、用量及最终文本能保存，内部思考和 Key 不导出。测试不新增智谱调用。

修复已部署到 8123，版本 1.4.2，PID 40124（实时身份以 runtime/server.json 为准）。综合核验 11 项通过，包含调用次数、原证据保留、源哈希、进程身份、健康、版本、设置和报告导出。模型、Key、输出/等待上限均未更改。

本次授权次数已用完，未做第二次请求。真实模型输出、整店外围语义与可用区域仍未验收；后续应先取得可完整核对的响应，再依据具体结束原因决定是否调整参数。修复不能事后证明本次失败根因或计费情况。

修改前 9 个文件备份及本次原实验哈希：`_archive/model-response-receipt-2026-09-10/manifest.json`。无提交或推送，可将本轮诊断修复与验证记录作为一个阶段保存。
